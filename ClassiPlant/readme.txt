ClassiPlant

This project was developed using Java and mysql
To run, open the ClassiPlantGUI file and hit run
alternatively, to run the text based version you can run the ClassiPlant class
however some functions may have gotten compromised while implementing the GUI
so it is preferred that you use the GUI
It is possible that the password in the Database folder must be changed
The .jar file is in the lib folder